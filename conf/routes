# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           	controllers.Application.index()

# References list
GET     /references                 	controllers.Application.list()

# Add reference
GET     /references/new              	controllers.Application.create()
POST    /references                  	controllers.Application.save()

# Bibtex-file
GET     /references/references.bib     	controllers.Application.generateBib()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               	controllers.Assets.at(path="/public", file)